# California Codes Platform - Production Environment Variables
# Copy this file to .env.production and fill in the actual values
#
# IMPORTANT: Keep this file secure and never commit it to version control!

# ============================================================================
# MongoDB Configuration
# ============================================================================
MONGO_USERNAME=admin
MONGO_PASSWORD=your-secure-mongodb-password-here

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_PASSWORD=your-secure-redis-password-here

# ============================================================================
# API Configuration
# ============================================================================
# Random API key for authentication (generate with: openssl rand -hex 32)
API_KEY=your-api-key-here

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================================================
# Firecrawl API Configuration (for ca-fire-pipeline)
# ============================================================================
# Get your API key from https://firecrawl.dev
FIRECRAWL_API_KEY=fc-your-firecrawl-api-key-here

# ============================================================================
# Notes
# ============================================================================
# 1. MongoDB and Redis passwords should be strong (at least 16 characters)
# 2. Use different passwords for dev/staging/production
# 3. The Firecrawl API key is only needed if you plan to run the pipeline
# 4. API_KEY is used for internal API authentication
# 5. On production server, copy this to .env.production with real values
